// как нe отображать % когда discont_price === null

// как ограничить на главной странице количество категорий до 4 шт

//  банер с гномом
    //градиент
    //rotation 180
    //расстояние между гномон и текстом с инпутами

// футер: как убрать рамку в iframe //  добавить в <iframe frameborder='no' ...>


//ProductsByCategorie - как вывести название категории?
 // на беке {}, запрос возвращает [] 
 поменять структуру под обьект?
 //  переписать редусер (заменить [] на {})
 // fetch заменить json.data на json
 // обработать state как {} // приходит undefined
 // пропсами прокинуть category, data до ProductsContainer
 // 


// pосле выноса http://localhost:3333/... в переменную - url - запросы работают, CategorieCard - нет